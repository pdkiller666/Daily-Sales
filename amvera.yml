meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"

build:
  systemPackages:
    - libzbar0

run:
  persistenceMount: /app/data
  command: python main.py
  containerPort: 5000
