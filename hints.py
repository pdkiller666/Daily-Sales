"""Система онбординга и контекстных подсказок.

Логика:
- Каждая подсказка показывается ровно один раз (хранится в user_hints_seen).
- hint_suffix() — добавляет короткую подсказку в конец текста при первом визите.
- maybe_send_welcome() — отправляет popup при первом входе (попап закрывается кнопкой).
"""

from __future__ import annotations
from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton


HINT_TEXTS: dict[str, str] = {
    'first_sale': (
        "💡 <i>Добавляйте несколько товаров в одну корзину — оформляется одним чеком. "
        "⭐ Избранное и 🔄 Недавние ускоряют выбор часто продаваемых позиций. "
        "Если товара нет в вашем магазине — продайте со склада другого магазина сети.</i>"
    ),
    'first_reports': (
        "💡 <i>Переключайте период кнопками Сегодня / Неделя / Месяц. "
        "Кнопка 🔍 Фильтр позволяет смотреть данные по конкретному магазину или городу. "
        "Раздел «📋 Мои планы» показывает ваш прогресс по целям за неделю и месяц.</i>"
    ),
    'first_products': (
        "💡 <i>Добавляйте товары вручную или загружайте сразу из Excel через 📊 Импорт. "
        "Остатки ведутся по каждому магазину отдельно — раздел 📦 Склад.</i>"
    ),
    'first_dashboard': (
        "💡 <i>Переключайте период — Сегодня / Неделя / Месяц. "
        "Администратор видит данные всех продавцов, выполнение планов и начисленные зарплаты. "
        "Фильтр позволяет сузить данные до конкретного магазина или города.</i>"
    ),
    'first_plans': (
        "💡 <i>Планы ставятся по продавцу или по магазину на неделю / месяц. "
        "При достижении 50%, 75% и 100% цели — автоматическое уведомление продавцу и администратору. "
        "Каждый milestone срабатывает строго один раз за период.</i>"
    ),
    'first_contests': (
        "💡 <i>Конкурс завершается автоматически в указанную дату — победитель определяется без ручного вмешательства. "
        "Результаты любого участника можно скорректировать вручную в любой момент. "
        "Фильтруйте зачётные продажи по категориям или магазинам.</i>"
    ),
    'first_rankings': (
        "💡 <i>Рейтинги обновляются в реальном времени — продавцы, магазины, города. "
        "Если вы не в топ-10 — ваша позиция всё равно показывается внизу списка. "
        "Переключайте периоды: 7 дней / месяц / прошлый месяц.</i>"
    ),
}

WELCOME_USER_TEXT = (
    "👋 <b>Добро пожаловать в ShopBot!</b>\n\n"
    "Вот что вам доступно:\n\n"
    "💰 <b>Продажи</b> — фиксируйте каждую продажу, корзина поддерживает несколько позиций одновременно\n"
    "⭐ <b>Избранное и 🔄 Недавние</b> — быстрый выбор часто продаваемых товаров\n"
    "📊 <b>Отчёты</b> — личная аналитика за сегодня, неделю и месяц\n"
    "📋 <b>Мои планы</b> — прогресс по целям продаж в реальном времени\n"
    "🏆 <b>Рейтинги</b> — ваше место среди коллег за 7 дней, месяц и прошлый период\n"
    "📦 <b>Склад</b> — остатки товаров по вашему магазину\n"
    "💵 <b>Заработок</b> — комиссия с продаж + оклад за смены\n"
    "🔔 <b>Уведомления</b> — настройте, какие события вам приходят\n"
    "👤 <b>Профиль</b> — часовой пояс, расписание смен, контакты, подписка\n\n"
    "Всё управляется кнопками — ничего лишнего. Удачных продаж! 🚀"
)

WELCOME_ADMIN_TEXT = (
    "🔑 <b>Вы — администратор организации!</b>\n\n"
    "Помимо стандартных функций вам доступно:\n\n"
    "👥 <b>Сотрудники</b> — инвайт-ссылка с пресетом роли и магазина; уведомление при каждой новой регистрации\n"
    "📅 <b>Дашборд</b> — сводка по всем продавцам и магазинам за любой период\n"
    "🏆 <b>Рейтинги</b> — лучшие продавцы, магазины и города\n"
    "📋 <b>Планы продаж</b> — цели по обороту / количеству с уведомлениями о прогрессе\n"
    "🎯 <b>Мотивация</b> — комиссии, оклады, расписание ставок по месяцам\n"
    "🏆 <b>Конкурсы</b> — мотивируйте команду соревнованием с автоопределением победителя\n"
    "🛍 <b>Товары</b> — каталог, импорт из Excel, управление остатками по магазинам\n"
    "🔔 <b>Рассылки</b> — уведомления всем, по магазину или по роли; поддержка отложенной отправки\n"
    "📊 <b>Google Sheets</b> — автоэкспорт продаж в таблицу (модуль «Интеграции»)\n\n"
    "Начните с приглашения сотрудников — раздел «👥 Сотрудники» → «🔗 Инвайт». 🚀"
)


def _dismiss_kb(hint_key: str) -> InlineKeyboardMarkup:
    return InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text="✅ Понятно!", callback_data=f"hint_dismiss:{hint_key}")]
    ])


def hint_suffix(db, user_id: int, hint_key: str) -> str:
    """Возвращает текст подсказки и помечает её как показанную.

    При первом визите → "\n\n💡 <i>...</i>".
    При повторных визитах → "" (экран остаётся чистым).
    Весь код завёрнут в try/except — подсказки никогда не ломают основной экран.
    Поддерживает как Database, так и AsyncDatabase (через sync-доступ к _db).
    """
    try:
        _sync = getattr(db, '_db', db)
        if _sync.has_seen_hint(user_id, hint_key):
            return ""
        _sync.mark_hint_seen(user_id, hint_key)
        text = HINT_TEXTS.get(hint_key, "")
        return f"\n\n{text}" if text else ""
    except Exception:
        return ""


async def maybe_send_welcome(message_or_callback, db, user_id: int, is_admin: bool) -> None:
    """Отправляет онбординг-попап при первом входе — ровно один раз.

    message_or_callback — aiogram Message или CallbackQuery.
    is_admin — True для владельцев / администраторов организации.
    Поддерживает как Database, так и AsyncDatabase (через sync-доступ к _db).
    """
    from aiogram.types import Message as _Msg
    hint_key = "welcome_admin" if is_admin else "welcome_user"
    try:
        _sync = getattr(db, '_db', db)
        if _sync.has_seen_hint(user_id, hint_key):
            return
        _sync.mark_hint_seen(user_id, hint_key)
        text = WELCOME_ADMIN_TEXT if is_admin else WELCOME_USER_TEXT
        if isinstance(message_or_callback, _Msg):
            await message_or_callback.answer(
                text, parse_mode="HTML", reply_markup=_dismiss_kb(hint_key)
            )
        else:
            await message_or_callback.message.answer(
                text, parse_mode="HTML", reply_markup=_dismiss_kb(hint_key)
            )
    except Exception:
        pass
