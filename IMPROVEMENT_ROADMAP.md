# DailySales — Карта улучшений и оптимизаций

> Составлен: 2026-06-25  
> Основан на глубоком анализе всего проекта: бот-ядро, веб-кабинет, БД-слой, биллинг, сквозные системы.  
> Код не изменялся — это чистый advisory-документ.  
> Связанные документы: `COMPETITIVE_ANALYSIS.md` (рынок/стратегия), `PROJECT_MAP.md` (архитектура), `.agents/memory/postgres-migration.md` (план миграции PostgreSQL).

---

## Контекст: почему продукт уже силён

| Метрика | Значение |
|---|---|
| Строк кода (Python) | 71 068 |
| Методов в Database | ~508 |
| Bot callback-роутеров | 707 |
| Web API маршрутов | 286 |
| Тестов | 723 / 723 ✅ |
| Индексов в БД | 56 составных |
| APScheduler-задач | 24 |

Большинство функциональных разрывов закрыто. Предложения ниже — **не «чего не хватает», а что сделает сильный продукт быстрее, надёжнее и доходнее**.

---

## 1. Здоровье кода и архитектура

| # | Находка | Влияние | Предложение |
|---|---|---|---|
| 1.1 | `database.py` — **15 771 строка, ~508 методов** в одном файле | Главный техдолг: трудно навигировать, тестировать, ревьюить; риск merge-конфликтов | Разбить на пакет `db/` по доменам (`db/sales.py`, `db/salary.py`, `db/tasks.py`…), `Database` собирать через миксины. Делается инкрементально |
| 1.2 | Handler-файлы-гиганты: `sales_handlers` 3 197 строк, `products` 2 753, `contests` 2 653, `reports` 2 366 | Высокая связность FSM + логики, трудно поддерживать | Разнести по подпапкам `handlers/sales/{commands,callbacks,fsm}.py` |
| 1.3 ✅ | Дублируется логика: инлайн-календарь, пагинация, кнопки «Назад» переписаны в нескольких модулях с вариациями | Расхождения в поведении, баги при точечных правках | ✅ _(2026-06-23)_ Создан `ui_components.py` — единая точка входа (реэкспорт `back_button`/`home_button`/`generate_calendar`/`safe_cb`/`resolve_cb_name` + `paginate`/`page_nav_row` + `back_row`); `back_button` получил опц. `text` (станд. «⬅️ Назад»); худшие нарушители (`tasks_handlers`, `integration_handlers`, `absence_handlers`, `payment_system_admin`) переведены на `page_nav_row()`/`back_button()` с сохранением callback_data |
| 1.4 ✅ | Джобы APScheduler объявлены прямо в `main.py` | `main.py` перегружен, трудно найти нужный job | ✅ _(2026-06-23)_ Inline-джобы вынесены в `jobs/registry.py` (`register_inline_jobs(...)` с явными аргументами); `main.py` 3365→1336 строк; поведение/ID/триггеры идентичны |
| 1.5 ✅ | Часть `dashboard_handlers` ходит в `sqlite3` напрямую, мимо `Database`-обёртки | Риск `database is locked` под конкуренцией | Унифицировать доступ через обёртку/пул |

---

## 2. Производительность и масштабирование

| # | Находка | Влияние | Предложение |
|---|---|---|---|
| 2.1 ✅ | Минутные джобы (`send_sales_alerts`, `send_payment_alerts`) перебирают **все** tenant-базы каждую минуту — O(N) по числу орг | При росте числа орг — latency-спайки и лишняя нагрузка на диск | ✅ _(2026-06-23)_ **Schedule Index** — событийный планировщик: индекс `{notif_type: {(hhmm,tz): [entries]}}` строится по dirty/TTL (15 мин), 4 минутных джоба читают индекс (O(уникальных (hhmm,tz))) вместо скана всех org-баз; инвалидация при смене настроек уведомлений (веб + бот) |
| 2.2 ✅ | Отчёты используют `date(sale_date)` в `WHERE` → индекс по строке `sale_date` **не используется** | Полный скан на больших таблицах продаж | ✅ _(2026-06-23)_ Добавлены **функциональные индексы** `CREATE INDEX ... ON sales(date(sale_date))` |
| 2.3 ✅ | Чат-FAB поллит сервер каждые 10 секунд | При многих пользователях — постоянный фоновый трафик | ✅ _(2026-06-23)_ Backoff при скрытой вкладке (`document.hidden`) + мгновенный refresh при возврате |
| 2.4 | SQLite single-writer потолок (~50 одновременных) | Технологический потолок роста | План миграции на PostgreSQL уже описан — запускать при ≥30 платящих орг или реальном lock-contention (см. `.agents/memory/postgres-migration.md`) |

---

## 3. База данных

| # | Находка | Предложение |
|---|---|---|
| 3.1 ✅ | Миграции — ручной паттерн «`PRAGMA table_info` + `ALTER TABLE`» без версионирования | Ввести лёгкую таблицу `schema_version` с пронумерованными шагами — уберёт класс багов «guarded ALTER при битом WAL» (инцидент уже был) |
| 3.2 | Hotspot-запросы в отчётах строятся конкатенацией строк | Перейти на параметризованный query-builder ради читаемости и однообразия |
| 3.3 | `_INITIALIZED_DBS` живёт только в памяти процесса | При hot-reload или деплое — повторный schema-check на первом запросе; некритично, но при частых деплоях шумит |

---

## 4. Веб-кабинет: UX и интерфейс

| # | Находка | Предложение |
|---|---|---|
| 4.1 ✅ | POS вылизан под тач, а модалка «Записать продажу» в разделе Продажи примитивнее: нет quick-qty кнопок | ✅ _(2026-06-23)_ Quick-qty кнопки модалки выровнены под POS-стиль (компактные бордер-пилюли `w-8 h-7 rounded-lg border`, набор `[1,2,3,5,10,20,50]`); одиночная продажа и endpoint `/sales/create` без изменений |
| 4.2 ✅ | Chart.js / HTMX / Alpine грузятся с внешних CDN (unpkg, jsdelivr) | ✅ _(2026-06-23)_ Библиотеки и шрифт Inter захостены локально в `web/static/vendor/`; CSP ужесточён (убраны unpkg/jsdelivr/google-fonts) |
| 4.3 ✅ | Тёмная тема держится на ~100 строках `!important`-оверрайдов в `base.html` | ✅ _(2026-06-23)_ Палитра тёмной темы вынесена в CSS-токены `html.dark{--ds-*}`; убраны лишние `!important` с class-utility оверрайдов (оставлены на element/input/table, где `dark:`-утилита иначе перебила бы по специфичности); 69 шаблонов не тронуты, рендер идентичен |
| 4.4 ✅ | Нет offline-страницы PWA — при потере сети генерик-ошибка браузера | ✅ _(2026-06-23)_ Добавлен брендированный offline-fallback в Service Worker |
| 4.5 ✅ | Доступность: много `<div>/<span>` с `@click` без ARIA-ролей и `tabindex`; контраст `text-slate-400` на `slate-800` ниже WCAG AA | Пройтись по интерактивным элементам (роли, фокус), поднять контраст вторичного текста |
| 4.6 ✅ | Везде «Loading…» текст при HTMX-запросах | ✅ _(2026-06-23)_ Заменено на CSS skeleton-лоадеры |
| 4.7 ✅ | Шрифт лендинга `Inter`, внутри кабинета — `system-ui` | ✅ _(2026-06-23)_ Inter подключён локально и в лендинге, и в кабинете (`base.html`) — единый шрифт |
| 4.8 | Changelog спрятан за кликом по логотипу — не обнаруживается | Бейдж «new» у номера версии в сайдбаре — выше вовлечённость в новые фичи |
| 4.9 ✅ | Фильтры/поиск «запоминаются» через nav-persistence script; нет явной кнопки сброса | Сделать кнопку «Сбросить» в шапке каждого модуля, либо двойной клик по пункту меню |

---

## 5. Экономика и монетизация

Модель «объём × функции» — сильная. Рычаги роста ARPU **без изменения архитектуры**:

| # | Идея | Ожидаемый эффект |
|---|---|---|
| 5.1 ✅ | **Годовая оплата модулей** — тарифы уже годовые, модули помесячные | ✅ _(2026-06-23)_ Годовой период добавлен как **опция рядом с месячной** (бот + веб + админка), явная выгода при оплате за год |
| 5.2 ✅ | **Inline-апселл при лимите** — на экране «50/50 товаров» сразу кнопка «+100 товаров» одним нажатием | ✅ _(2026-06-23)_ Реализовано — CTA на докупку в точке лимита |
| 5.3 ✅ | **Grace-period 3–5 дней (soft-expiry)** — фичи работают с предупреждением | ✅ _(2026-06-23)_ Grace-period 3 дня: доступ + баннер «оплатите» после end_date |
| 5.4 ✅ | **Перетюнить скидки бандлов** — маленький бандл −20% слаб (выгоднее взять 2 модуля отдельно) | Оптимум −30–40% на мелких пакетах; «Всё включено» −45% уже хорошо |
| 5.5 | **AI по кредитам/токенам**, а не флэт-модуль — стоимость провайдера переменная | Usage-тиры уже частично есть (`ai_rate_config`); масштабировать тиры лимитов |
| 5.6 ✅ | **Lite / Pro версии модулей** (напр. Аналитика: 3 отчёта vs полный набор) | Снижает порог входа для самого дешёвого сегмента |
| 5.7 | **Платный онбординг для сетей** (разово 3–5k₽) | Near-zero COGS → чистая маржа; нужен только оффер на лендинге |
| 5.8 ✅ | **Реферальная программа** — давать бонус-модуль на месяц, а не только продление подписки | Модуль как бонус ценнее воспринимается, выше вирусность |
| 5.9 ✅ | **Мультивалютность (KZT, BYR, UZS)** | СНГ-рынок с низкой конкуренцией в нише | ✅ _(2026-06-25)_ `currency_utils.py` (12 валют: RUB/KZT/BYN/UZS/AMD/GEL/AZN/KGS/TJS/TMT/USD/EUR); `CurrencyMiddleware` декодирует JWT → кэш; `@pass_context` на `fmt_currency` — все старые шаблоны без изменений; настройка в `/settings` (owner); `tenant_manager.py` migration `currency DEFAULT 'RUB'` |

**Текущий ARPU-потолок: ~1 600₽/мес.** При реализации п.5.1–5.4 — реалистично +20–30% без единого нового клиента.

---

## 6. Безопасность

| # | Находка | Предложение | Срочность |
|---|---|---|---|
| 6.1 ✅ | При отсутствии `WEB_SECRET_KEY` секрет выводится из `BOT_TOKEN` | ✅ _(2026-06-23)_ Явный `WEB_SECRET_KEY` задан в Secrets (⚠️ добавить и в прод-окружение Amvera) | 🔴 Высокая |
| 6.2 ✅ | CSRF-токен детерминированный `hmac(secret, jwt)` — одинаков для всей сессии | ✅ _(2026-06-23)_ Per-request токен `<nonce>.<hmac(secret, jwt+nonce)>`; verify читает form-поле и `X-CSRF-Token` header; back-compat со старым форматом | 🟡 Средняя |
| 6.3 ✅ | `Europe/Moscow` как fallback TZ захардкожен в нескольких местах | ✅ _(2026-06-23)_ Сведено в одну константу `DEFAULT_TZ` в `timezone_utils` | 🟢 Низкая |
| 6.4 ✅ | `try/except: pass` в нескольких местах `web/app.py` — молчат ошибки конфигурации/БД | ✅ _(2026-06-23)_ Тихие `except` в критичных местах логируются на уровне WARNING (контрол-флоу не изменён) | 🟡 Средняя |
| 6.5 ✅ | — | ✅ _(2026-06-23)_ **2FA (TOTP) для owner + recovery-коды + аудит-лог супер-админа** (`/admin/audit`); хуки на grant/revoke биллинга, удаление орг, сброс подписки, бэкапы | 🟡 Средняя |

---

## 7. Сквозные системы

### AI-аналитика
- **Что хорошо:** fallback DeepSeek→Gemini→OpenRouter, circuit breaker 180с, учёт токенов/стоимости по фичам.
- **Пробел:** нет тестов на сам fallback (через моки провайдеров).
- **Предложение:** структурированное (JSON) логирование для анализа AI-latency и частоты fallback.

### Google Sheets
- **Проблема:** хрупкая привязка к структуре листа (A1-нотация, индексы колонок). Вставка колонки пользователем ломает синк.
- **Предложение:** добавить слой маппинга «системное поле → колонка листа» — тогда пользователь сможет переименовывать и переставлять столбцы без поломки интеграции.

### Email / SMTP
- SMTP-хост (`smtp.yandex.ru`) захардкожен в коде.
- **Предложение:** вынести хост, порт и шифрование в конфиг — для смены провайдера без правки кода.

### Уведомления
- Покрытие хорошее: Telegram + Web Push + Email + запланированные.
- **Пробел:** нет дедупликации — пользователь может получить одно событие и через бот, и через push, если оба канала активны.
- **Предложение:** единая таблица «уведомление отправлено по каналу X» для дедупликации.

---

## 8. Тестирование

| Текущее состояние | Пробел | Предложение |
|---|---|---|
| 723 функциональных теста | Нет тестов AI-fallback (через моки провайдеров) | Добавить mocked-тесты для каждого уровня chain |
| — | Нет тестов thread-safety пула соединений | Concurrency-тест: 20 потоков пишут в одну org_*.db |
| — | Rate-limiting при locked SQLite не покрыт | Тест fail-closed поведения rate_store |
| — | Нет интеграционных тестов web-роутов (только unit) | Минимальный smoke-test каждого маршрута через httpx.AsyncClient |

---

## 9. Стратегические направления (долгосрок)

| Направление | Что даёт | Сложность |
|---|---|---|
| **REST API / Webhook-out** | Открывает 1С, маркетплейсы, интернет-магазины; самый широкий мультипликатор аудитории | Высокая |
| **PostgreSQL-миграция** | Снимает потолок 50 одновременных; горизонтальное масштабирование | Очень высокая (план в `.agents/memory/postgres-migration.md`) |
| **Базовая CRM (клиентская база)** | LTV × 3–5; программа лояльности как дифференциатор | Высокая |
| **Закупки / поставщики** | Замыкает цикл: продажи → остатки → заказ поставщику | Средняя |
| **54-ФЗ / ОФД** | Позволяет быть основной кассой для B2C наличных | Очень высокая (регуляторика) |

---

## 10. Сводная приоритизация

### ✅ Реализовано в заходе 2026-06-23 (последний батч)

- ✅ **1.5** — dashboard переведён на пул-обёртку `Database` (WAL + thread-safe), убраны прямые `sqlite3.connect`
- ✅ **3.1** — версионирование миграций: таблица `schema_version` + нумерованные шаги, baseline на первом старте
- ✅ **4.5** — доступность: ARIA-роли/`tabindex`/keydown на интерактивах, поднят контраст вторичного текста
- ✅ **4.9** — кнопка «Сбросить фильтры» в шапке модулей с фильтрами
- ✅ **5.4** — перетюнены скидки бандлов (идемпотентно): small_biz 399→329, team 699→679, all_in_one 1299→1199
- ✅ **5.6** — Lite/Pro для Аналитики: Lite = голый модуль, Pro = бандл `analytics_pro` (599₽), без изменений гейтов
- ✅ **5.8** — реферальный бонус-модуль (Аналитика 30 дн.) + **фикс мёртвой колонки** (`bonus_granted`→`applied`, реф-бонусы не работали) + атомарный claim + concurrency-тест

### 🟢 Быстрые победы (мало кода, высокая отдача — делать первыми)

1. ✅ Явный `WEB_SECRET_KEY` в Secrets _(2026-06-23)_
2. ✅ Функциональные индексы на `date(sale_date)` в sales _(2026-06-23)_
3. ✅ Годовая оплата модулей (опция рядом с месячной, явная выгода; бот + веб + админка) _(2026-06-23)_
4. ✅ Inline-апселл при достижении лимита _(2026-06-23)_
5. ✅ Grace-period 3 дня (soft-expiry) _(2026-06-23)_
6. ✅ Offline-страница PWA _(2026-06-23)_
7. ⏭️ Бейдж «new» у версии в сайдбаре — **пропущено по решению пользователя**
8. ✅ Skeleton-лоадеры вместо «Loading…» _(2026-06-23)_
9. ✅ Централизовать TZ-fallback в одну константу _(2026-06-23)_

### 🟡 Среднесрок (рефакторинг и качество)

10. Декомпозиция `database.py` → пакет `db/`
11. Разбивка handler-гигантов по подпапкам
12. ✅ Унификация POS/Продажи: quick-qty модалки выровнены под POS-стиль _(2026-06-23)_
13. ✅ Локальный хостинг CDN-библиотек _(2026-06-23)_
14. Слой маппинга Google Sheets
15. ✅ Структурное JSON-логирование (AI latency, fallback) _(2026-06-23)_
16. ✅ 2FA owner + аудит-лог супер-админа _(2026-06-23)_
17. AI usage-tiers (кредиты/токены)
18. ✅ Тесты: AI-fallback, concurrency, rate-limit fail-closed _(2026-06-23)_

### 🔴 Стратегия (снятие потолков, новые сегменты)

19. PostgreSQL-миграция
20. REST API / Webhook-out
21. ✅ Событийный планировщик — Schedule Index (вместо O(N)-обхода всех орг каждую минуту) _(2026-06-23)_
22. Базовая CRM
23. Мультивалютность (СНГ)

---

### 🔭 Что осталось (открытый бэклог — без ✅)

- **Архитектура:** 1.1 декомпозиция `database.py`, 1.2 разбивка handler-гигантов
- **Производительность:** 2.4 миграция на PostgreSQL (по триггеру ≥30 платящих орг)
- **БД:** 3.2 параметризованный query-builder, 3.3 персист `_INITIALIZED_DBS`
- **UX:** 4.8 бейдж «new» у версии _(пропущено по решению пользователя)_
- **Монетизация:** 5.5 AI по кредитам/токенам, 5.7 платный онбординг сетей
- **Сквозное:** маппинг полей Google Sheets, вынос SMTP-конфига, дедупликация уведомлений по каналам
- **Стратегия:** REST API / Webhook-out, базовая CRM, закупки/поставщики, 54-ФЗ/ОФД

---

*Документ обновляется по мере реализации пунктов. При закрытии пункта — добавить ✅ и дату. Последнее обновление: 2026-06-26.*
